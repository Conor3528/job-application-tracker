info:
  name: Filter by Status
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:8080/api/applications?status=APPLIED
  params:
    - name: status
      value: APPLIED
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
