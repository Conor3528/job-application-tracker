info:
  name: Get All Applications
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:8080/api/applications
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
