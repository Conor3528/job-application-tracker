info:
  name: Create Application (invalid)
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:8080/api/applications
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
